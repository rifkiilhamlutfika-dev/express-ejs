<%- include('partials/header',{dev:'random'}) %> <%- include('partials/navbar')
%>
<h1>Random number: <%= number %></h1>
<% if(number % 2 === 0) { %>
<h3>Angka Genap</h3>
<% } else { %>
<h3>Angka Ganjil</h3>
<% } %>

<h3>That's number is <%= number % 2 === 0 ? 'Even' : 'Odd' %></h3>
<%- include('partials/footer') %>
